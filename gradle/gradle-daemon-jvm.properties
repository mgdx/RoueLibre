#This file is generated by updateDaemonJvm
#
#The `toolchainUrl.*` entries that task writes are deliberately stripped: they
#point at api.foojay.io so Gradle can download a daemon JDK, and F-Droid's
#scanner refuses a build able to fetch one. Their image carries JDK 21, the
#version asked for below, so nothing has to be downloaded. Whoever re-runs
#`updateDaemonJvm` has to strip them again.
toolchainVersion=21
