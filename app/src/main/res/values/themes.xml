<?xml version="1.0" encoding="utf-8"?>
<!--
    The application's theme — SPEC.md §7.

    Material Components serves as the technical base, but none of its default
    colours survives: every role points at the project's colour tokens. The
    dark theme is described in values-night/ and receives the same care; it is
    not an automatic inversion.
-->
<resources>

    <style name="Theme.RoueLibre" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- The signal hue carries interaction as well as availability: the
             boldness is spent in a single place. -->
        <item name="colorPrimary">@color/signal</item>
        <item name="colorOnPrimary">@color/surface</item>
        <item name="colorSecondary">@color/ink_soft</item>
        <item name="colorOnSecondary">@color/surface</item>
        <item name="colorError">@color/alert</item>
        <item name="colorOnError">@color/surface</item>

        <item name="android:colorBackground">@color/paper</item>
        <item name="colorOnBackground">@color/ink</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/ink</item>
        <item name="colorSurfaceVariant">@color/paper</item>
        <item name="colorOnSurfaceVariant">@color/ink_soft</item>
        <item name="colorOutline">@color/outline</item>
        <item name="colorOutlineVariant">@color/outline</item>
        <!-- The five container roles of Material 3, mapped onto the project's
             two tones. Leaving them unset is not neutral: the components that
             read them — the station sheet first of all — fall back on the
             library's baseline, a mauve-tinted grey that has nothing to do
             with this palette. A component needing a third tone carries its
             own state list, as the toggle and the search field do. -->
        <item name="colorSurfaceContainerLowest">@color/surface</item>
        <item name="colorSurfaceContainerLow">@color/surface</item>
        <item name="colorSurfaceContainer">@color/surface</item>
        <item name="colorSurfaceContainerHigh">@color/surface</item>
        <item name="colorSurfaceContainerHighest">@color/surface</item>
        <item name="colorSurfaceDim">@color/paper</item>
        <item name="colorSurfaceBright">@color/surface</item>
        <!-- Material tints a raised surface with this colour in proportion to
             its elevation. Pointing it at the surface itself cancels that
             tint: here the separation between two planes is a change of tone,
             never a shadow and never a wash of the signal hue. -->
        <item name="elevationOverlayColor">@color/surface</item>
        <!-- The container pairs. The palette has one signal hue, so the
             tertiary roles repeat the primary rather than introducing a third
             colour Material would otherwise invent. -->
        <item name="colorPrimaryContainer">@color/availability_low</item>
        <item name="colorOnPrimaryContainer">@color/ink</item>
        <item name="colorSecondaryContainer">@color/paper</item>
        <item name="colorOnSecondaryContainer">@color/ink</item>
        <item name="colorTertiary">@color/signal</item>
        <item name="colorOnTertiary">@color/surface</item>
        <item name="colorTertiaryContainer">@color/availability_low</item>
        <item name="colorOnTertiaryContainer">@color/ink</item>
        <item name="colorErrorContainer">@color/paper</item>
        <item name="colorOnErrorContainer">@color/alert</item>
        <!-- Inverted surfaces: the alert bar shows on a background opposite
             to the screen's. Without these three roles, Material would bring
             its default purple back there. -->
        <item name="colorSurfaceInverse">@color/surface_inverse</item>
        <item name="colorOnSurfaceInverse">@color/ink_inverse</item>
        <item name="colorPrimaryInverse">@color/signal_inverse</item>

        <item name="android:statusBarColor">@color/paper</item>
        <item name="android:navigationBarColor">@color/paper</item>
        <item name="android:windowLightStatusBar">true</item>

        <item name="android:textAppearance">@style/TextAppearance.RoueLibre.Body</item>
        <item name="textAppearanceTitleLarge">@style/TextAppearance.RoueLibre.Title</item>
        <item name="textAppearanceBodyLarge">@style/TextAppearance.RoueLibre.Body</item>
        <item name="textAppearanceBodyMedium">@style/TextAppearance.RoueLibre.Secondary</item>
        <item name="textAppearanceLabelMedium">@style/TextAppearance.RoueLibre.Label</item>

        <item name="materialButtonStyle">@style/Widget.RoueLibre.Button</item>
        <!-- Running text is justified, application-wide (SPEC §7). -->
        <item name="android:textViewStyle">@style/Widget.RoueLibre.Text</item>

        <!-- A sheet is dressed by its own theme, and the line above does not
             reach it. See ThemeOverlay.RoueLibre.BottomSheetDialog. -->
        <item name="bottomSheetDialogTheme">@style/ThemeOverlay.RoueLibre.BottomSheetDialog</item>

        <!-- A single radius, applied to every shape family. -->
        <item name="shapeAppearanceCornerExtraSmall">@style/ShapeAppearance.RoueLibre</item>
        <item name="shapeAppearanceCornerSmall">@style/ShapeAppearance.RoueLibre</item>
        <item name="shapeAppearanceCornerMedium">@style/ShapeAppearance.RoueLibre</item>
        <item name="shapeAppearanceCornerLarge">@style/ShapeAppearance.RoueLibre</item>
        <item name="shapeAppearanceCornerExtraLarge">@style/ShapeAppearance.RoueLibre</item>
    </style>

    <!--
        The theme the window is born with, and only that — SPEC §7.0.

        Between the tap on the launcher icon and the activity's first frame,
        Android draws a window of its own from the theme declared in the
        manifest. Left on Theme.RoueLibre that window is `paper`, so the
        opening green arrived after a pale flash. This theme paints the
        identity's green there instead, system bars included, so the green is
        already in place when the activity takes over and the opening reads as
        one continuous screen rather than two.

        It follows nothing: no values-night variant, because the mark it
        carries has one appearance (see identity.xml).

        MainActivity puts Theme.RoueLibre back before its first inflation —
        this theme must never dress the interface itself.
    -->
    <style name="Theme.RoueLibre.Intro" parent="Theme.RoueLibre">
        <item name="android:windowBackground">@color/identity_ground</item>
        <item name="android:statusBarColor">@color/identity_ground</item>
        <item name="android:navigationBarColor">@color/identity_ground</item>
        <!-- White bars on a dark green ground. -->
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!--
        The theme a sheet sliding up from the bottom is dressed by.

        It exists to undo one inheritance. Material's bottom sheet theme
        descends from its **dialog** overlay, which points `materialButtonStyle`
        at the style of an alert dialog's buttons — and that style carries
        `android:lines="1"`, `android:singleLine` and `android:ellipsize="end"`,
        decided for a pair of buttons reading "OK" and "Cancel". A style quoted
        in a layout only overrides the attributes it names, so the three buttons
        of a station's sheet kept their colours and their shape from
        `Widget.RoueLibre.Toggle` and took the line limit from the theme: at
        ×1.3 in French "Ouvrir dans une appli de navigation" came out as
        "Ouvrir dans une appli de navig…", and at ×2.0 "Partir d'ici" as
        "Partir d'i…". SPEC §7 rules that out — it is the height that gives way,
        never the size of the characters, and a label written away is neither.

        So the application's own button style is put back where every other
        screen has it. Nothing else changes: the sheets' buttons all quote
        `Widget.RoueLibre.Toggle`, which decides everything one sees, and a
        button's layout margins are read from the layout rather than from this
        style. What comes back with it is the absence of a line limit.
    -->
    <style name="ThemeOverlay.RoueLibre.BottomSheetDialog"
        parent="ThemeOverlay.Material3.BottomSheetDialog">
        <item name="materialButtonStyle">@style/Widget.RoueLibre.Button</item>
    </style>

    <style name="ShapeAppearance.RoueLibre" parent="ShapeAppearance.Material3.Corner.None">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">@dimen/radius</item>
    </style>

</resources>
